
# Write a function called `search` which takes in 2 parameters:
#     1. arr which is the array
#     2. target which is the value to be search in the array
def search(arr,target):
     if target in arr:
          print("exists")
    else:
        print("no")


